{
    "version": 2,
    "builds": [
      {
        "src": "mkdocs.yml",
        "use": "@vercel/static-build",
        "config": {
          "buildCommand": "mkdocs build --clean",
          "distDir": "site"
        }
      }
    ]
  }